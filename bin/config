--
-- Configurations
--

-- start-up service
start_name = 'test'
start_file = '../test/runner.lua'

-- lua module searching path
lua_path = './?.lua;../lib/?.lua;../test/?.lua'
lua_cpath = '../lib/?.so;../lib/?.dll'

-- Max size of an inproc message, limited to 64M
max_ipc_msg_size = 64 * 1024 * 1024
max_recv_timeout = -1
router_mandatory = 1

-- high water marks
recv_hwm = 2048
send_hwm = 2048

log_to_file = 1
